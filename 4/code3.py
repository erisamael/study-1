def sort(x,reverse=False):
	newlist = []
	if reverse = True
		for i in range(len(x)):
			newlist.append(max(x))
			x.remove(max(x))
	else:
		for i in range(len(x)):
			newlist.append(min(x))
			x.remove(min(x))
	return newlist
