def sort(x):
	newlist = []
	for i in range(len(x)):
		newlist.append(mdef sort(x):
	newlist = []
	for i in range(len(x)):
		newlist.append(max(x))
		x.remove(max(x))
		return newlist
ax(x))
		x.remove(max(x))
		return newlist
